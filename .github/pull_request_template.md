## Summary

<!-- What does this PR change and why? -->

Closes #

## Changes

-

## How was this tested?

<!-- Commands run, new tests, manual checks. For bug fixes: show that the
regression test fails without the fix. -->

## Checklist

- [ ] Conventional Commit title (`feat:`, `fix:`, `docs:`, ...)
- [ ] Tests added or updated; core logic written test-first
- [ ] Bug fix: regression test verified to fail without the fix
- [ ] `uv run pre-commit run --all-files` passes
- [ ] `CHANGELOG.md` updated under `[Unreleased]` (if user-visible)
- [ ] Docs / ADR updated (if behaviour or a decision changed)
- [ ] Invariants respected: core imports no adapter, hook never breaks the agent, no network, no `shell=True`
